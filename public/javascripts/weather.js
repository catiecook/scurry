$('dashboard.hbs').ready(function(e) {
  event.preventDefault();

  var $urlBase = 'http://api.wunderground.com/api/';
  var weatherAPI = weatherAPI;
  var $zip = /*pull in zipcode from user info */


})
